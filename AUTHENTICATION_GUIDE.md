# دليل نظام تسجيل الدخول / Authentication Guide

## نظرة عامة / Overview

تم تنفيذ نظام تسجيل دخول بسيط مع دورين للمستخدمين:
- **Admin (مدير النظام)**: صلاحيات كاملة
- **Secretary (سكرتيرة)**: صلاحيات محدودة

## معلومات الدخول الافتراضية / Default Login Credentials

بعد تشغيل السيد (seed):

```
Admin:
  Username: admin
  Password: admin123

Secretary 1:
  Username: secretary1
  Password: secretary123

Secretary 2:
  Username: secretary2
  Password: secretary123
```

## الصلاحيات / Permissions

### Admin (مدير النظام)
- الوصول الكامل لجميع الصفحات والميزات
- إدارة المستخدمين (إضافة، تعديل، حذف)
- الوصول إلى التقارير المالية
- إدارة الفواتير والمصروفات
- إدارة الإعدادات
- التقاضي الإلكتروني والخدمات الإلكترونية

### Secretary (سكرتيرة)
- عرض وإضافة وتعديل الموكلين
- عرض القضايا
- عرض وإدارة الجلسات
- عرض وإضافة المستندات
- عرض وإدارة المواعيد
- عرض فهرس المحاكم

**لا يمكن للسكرتيرة:**
- الوصول إلى الفواتير والمدفوعات
- الوصول إلى المصروفات
- عرض التقارير
- إدارة المستخدمين
- تعديل الإعدادات
- الوصول إلى التقاضي والخدمات الإلكترونية

## البنية التقنية / Technical Structure

### الملفات الرئيسية / Main Files

1. **electron/database/models/User.js**
   - نموذج المستخدم مع الأدوار (admin, secretary)

2. **electron/database/services/userService.js**
   - خدمات إدارة المستخدمين (إنشاء، تعديل، حذف، مصادقة)

3. **electron/main.js**
   - معالجات IPC للمصادقة (login, logout, getCurrentUser)

4. **src/contexts/AuthContext.jsx**
   - سياق React للمصادقة
   - إدارة حالة المستخدم الحالي

5. **src/pages/Login.jsx**
   - واجهة تسجيل الدخول

6. **src/pages/Users.jsx**
   - صفحة إدارة المستخدمين (للمدير فقط)

7. **src/utils/api.js**
   - واجهات API للمصادقة

### الحماية / Security

- كلمات المرور مشفرة باستخدام bcrypt (10 rounds)
- التحقق من الجلسة عند كل تحميل للتطبيق
- حماية المسارات باستخدام ProtectedRoute component
- تصفية القوائم الجانبية بناءً على دور المستخدم

## كيفية الاستخدام / How to Use

### تسجيل الدخول

1. عند بدء التطبيق، سيتم عرض شاشة تسجيل الدخول
2. أدخل اسم المستخدم وكلمة المرور
3. سيتم توجيهك إلى لوحة التحكم بناءً على صلاحياتك

### إضافة مستخدم جديد (للمدير فقط)

1. انتقل إلى "إدارة المستخدمين" من القائمة الجانبية
2. انقر على "إضافة مستخدم جديد"
3. املأ البيانات المطلوبة:
   - الاسم الكامل (مطلوب)
   - اسم المستخدم (مطلوب)
   - كلمة المرور (مطلوبة)
   - البريد الإلكتروني (اختياري)
   - رقم الهاتف (اختياري)
   - الدور (Admin أو Secretary)
   - الحالة (نشط أو معطل)
4. انقر على "إضافة"

### تعديل مستخدم

1. في صفحة "إدارة المستخدمين"
2. انقر على زر التعديل (✏️) بجانب المستخدم
3. عدّل البيانات المطلوبة
4. اترك حقل كلمة المرور فارغاً للإبقاء على الكلمة الحالية
5. انقر على "تحديث"

### حذف/تعطيل مستخدم

- **تعطيل**: انقر على زر القفل (🔒/🔓)
- **حذف**: انقر على زر الحذف (🗑️)

### تسجيل الخروج

انقر على "تسجيل الخروج" (🚪) في أسفل القائمة الجانبية

## التخصيص / Customization

### تغيير الصلاحيات

لتخصيص صلاحيات السكرتيرة، عدّل ملف `src/contexts/AuthContext.jsx`:

```javascript
const secretaryPermissions = [
  "view_clients",
  "add_clients",
  // أضف المزيد من الصلاحيات هنا
];
```

### إضافة دور جديد

1. عدّل `electron/database/models/User.js` لإضافة الدور الجديد
2. عدّل `src/App.jsx` لتحديث القوائم المتاحة
3. عدّل `src/contexts/AuthContext.jsx` لإضافة الصلاحيات

## الأمان / Security Notes

⚠️ **مهم**:
- غيّر كلمات المرور الافتراضية فوراً في بيئة الإنتاج
- استخدم كلمات مرور قوية
- راجع سجلات الدخول بانتظام
- لا تشارك معلومات الدخول

## الدعم / Support

للمزيد من المساعدة أو للإبلاغ عن مشاكل، يرجى مراجعة التوثيق الرئيسي للمشروع.
